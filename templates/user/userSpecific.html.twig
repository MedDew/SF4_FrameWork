{# empty Twig template #}
{% extends 'base.html.twig' %}

{% block title %}User view :{{user.lastName}} {% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>User Specific features✅</h1>

    <div>
        <p>You're viewing the following user features : </p>
        <ul>
            <li>{{user.id}}</li>
            <li>{{user.lastName}}</li>
            <li>{{user.firstName}}</li>
            <li>{{user.creationDate}}</li>
            <li>{{user.lastLoginDate}}</li>
            <li>{{user.isLogged²}}</li>
        </ul>
    </div>
</div>
{% endblock %}